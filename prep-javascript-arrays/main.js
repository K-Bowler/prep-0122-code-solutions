const languages = [
  'html',
  'css',
  'javascript',
  'react',
  'node',
  'postgres'
];

console.log('Languages:', languages);

var firstElement = languages[0];
console.log('The fist element in the array is:', firstElement);

var thirdElement = languages[2];
console.log('The third element of the array is:', thirdElement);

var length = languages.length;
console.log('The length of the array:', length);

var lastElement = languages[languages.length - 1];
console.log('The last element in the array is:', lastElement);
